# Dependency declarations now live in pyproject.toml; exact pinned versions live in
# requirements.lock.txt.
#
# This file is kept only as a pointer. It used to be a third, independent copy of the version
# bounds, and it went stale exactly the way config_a100.yaml did before `extends:` was introduced:
# the working environment violated SIX of its caps (tree-sitter 0.25.2 against `<0.24.0`, xgboost
# 3.3.0 against `<3.0.0`, scipy 1.15 against `<1.14.0`, scikit-learn 1.6.1 against `<1.6.0`,
# pyarrow 24 against `<18.0.0`, huggingface_hub 1.27 against `<1.0.0`) while devign_data/parser.py
# named 0.25.2 + 0.24.2 as the verified pair. Two sources of truth is already one too many.
#
# Reproduce the measured environment:
#
#   pip install torch==2.5.1+cu121 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.lock.txt
#
# Or install the project with looser, still-valid bounds:
#
#   pip install -e .
#
-e .
